# Skills

The following skills extend your capabilities.

Use `skills_list` first to discover matching skills, then use `skill_view` to read the full `SKILL.md` or one allowed supporting file.
Prefer these dedicated skill tools over guessing file paths with `read_file`.
If you discover a reusable workflow worth preserving, or a generated skill needs correction, use `skill_manage` instead of generic file-writing tools.

Skills with `available="false"` need dependencies installed first.

{{ skills_summary }}
